#include <iostream>
#include <fstream>
#include <string>

struct banco{
    
};
int main(){
    banco op;
    do{
        switch(op){
        case : 
        }
    } while (op!=0);
    
    return 0;
}
